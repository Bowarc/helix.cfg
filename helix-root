base="$HOME/.config/helix/config.toml"
generated="$HOME/.config/helix/root.toml"
theme="gruvbox_dark_hard"

sed -E \
  "s/^([[:space:]]*theme[[:space:]]*=[[:space:]]*).*/\1\"$theme\"/" \
  "$base" > "$generated"

sudo hx --config "$generated" "$@"

rm $generated
